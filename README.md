Steps to run:

Step 1:Open vscode and go to the folder u want.Run:
    git clone "https://github.com/RamPrasath-12/ShopWhatYouSee.git:

Step 2:open backend folder and create virtual environment :
   python -m venv venv

Step 3:activate the virtual environment
   .\venv\Scripts\\activate(for windows)

Step 4:Run :
 pip install -r requirements.txt

 Step 5:install postgresql database .
 
 step 6:In backend folder run :
 python tools/import_products.py

 Step 7:Open frontend folder.Install node if don't have.Run:
 npm install

 Step 8:Run backend:
 python app.py

 Step 9:Run frontend:
 npm run dev

